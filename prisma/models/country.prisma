model Country {
  id        Int      @id @default(autoincrement())
  name     String    @unique
  createdBy  Int
  updatedBy  Int
  createdAt DateTime @default(now())
  updatedAt DateTime? @updatedAt 
  status    Boolean

  @@map("countries")
}
